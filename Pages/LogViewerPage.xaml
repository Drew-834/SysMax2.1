<Page x:Class="SysMax2._1.Pages.LogViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SysMax2._1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="System Logs">

    <Page.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980b9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1f6aa5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogTextStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10"/>
        </Style>

        <Style x:Key="FilterButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#2d2d2d"/>
            <Setter Property="Foreground" Value="#BBBBBB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center" 
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#3498db"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <TextBlock Text="System Logs" FontSize="24" Foreground="White" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="RefreshButton" Content="Refresh" Style="{StaticResource ActionButtonStyle}" Click="RefreshButton_Click"/>
                    <Button x:Name="ExportButton" Content="Export Logs" Style="{StaticResource ActionButtonStyle}" Margin="10,5,0,5" Click="ExportButton_Click"/>
                </StackPanel>
            </Grid>

            <!-- Filter Controls -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,15,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Log Filters" Style="{StaticResource CardHeaderStyle}"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Show:" Foreground="#BBBBBB" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <ToggleButton x:Name="InfoFilter" Content="Info" IsChecked="True" Style="{StaticResource FilterButtonStyle}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <ToggleButton x:Name="WarningFilter" Content="Warning" IsChecked="True" Style="{StaticResource FilterButtonStyle}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <ToggleButton x:Name="ErrorFilter" Content="Error" IsChecked="True" Style="{StaticResource FilterButtonStyle}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                            <ToggleButton x:Name="CriticalFilter" Content="Critical" IsChecked="True" Style="{StaticResource FilterButtonStyle}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                        </StackPanel>

                        <TextBox x:Name="SearchBox" Grid.Column="1" Margin="10,0" Padding="10,5" 
                                Background="#333333" Foreground="White" BorderThickness="0"
                                Text="Search logs..." GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus" 
                                TextChanged="SearchBox_TextChanged"/>

                        <ComboBox x:Name="TimeRangeComboBox" Grid.Column="2" Width="150" 
                                 Background="#333333" Foreground="White" BorderThickness="0"
                                 SelectedIndex="0" SelectionChanged="TimeRangeComboBox_SelectionChanged">
                            <ComboBoxItem Content="Recent logs"/>
                            <ComboBoxItem Content="Today"/>
                            <ComboBoxItem Content="Yesterday"/>
                            <ComboBoxItem Content="Last 7 days"/>
                            <ComboBoxItem Content="All logs"/>
                        </ComboBox>
                    </Grid>
                </Grid>
            </Border>

            <!-- Log Content -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,15,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <TextBlock Text="Log Entries" Style="{StaticResource CardHeaderStyle}" Margin="0"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="CopyButton" Content="Copy to Clipboard" Padding="10,5" 
                                   Background="#2d2d2d" Foreground="White" BorderThickness="0" Click="CopyButton_Click"/>
                            <Button x:Name="ClearButton" Content="Clear Display" Padding="10,5" Margin="10,0,0,0"
                                   Background="#2d2d2d" Foreground="White" BorderThickness="0" Click="ClearButton_Click"/>
                        </StackPanel>
                    </Grid>

                    <TextBox x:Name="LogTextBox" Grid.Row="1" Style="{StaticResource LogTextStyle}" 
                            IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                            HorizontalScrollBarVisibility="Auto" AcceptsReturn="True" Margin="0,10,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>